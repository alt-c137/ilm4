# Ruff — линтер/форматтер ilm4.
# RUF012 отключён: Meta.ordering и choices в Django — легитимные
# изменяемые атрибуты класса, ложное срабатывание для Django-проектов.
# DTZ отключён: времена намаза — наивные datetime осознанно, они в локальной
# зоне координат (смещение передаётся числом, строковые 'HH:MM' из praytimes).
line-length = 120

[lint]
ignore = ["RUF012", "DTZ"]
