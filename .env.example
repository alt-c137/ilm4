DJANGO_SETTINGS_MODULE=config.settings.dev
SECRET_KEY=change-me

# БД: пока строка закомментирована — работает SQLite (не нужен Docker).
# Когда включишь Docker Desktop → Settings → Resources → WSL Integration → Ubuntu,
# подними сервисы (`docker compose up -d`) и раскомментируй:
# DATABASE_URL=postgis://ilm4:ilm4@localhost:5432/ilm4

# Фаза 1 (accounts):
# GOOGLE_OAUTH_CLIENT_ID=
# GOOGLE_OAUTH_CLIENT_SECRET=
